apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-nfs-pvc
  namespace: main
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: nfs
